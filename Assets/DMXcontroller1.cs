using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DMXcontroller1 : MonoBehaviour
{
    [SerializeField] bool working = true;

    
    
    public byte channel1 = 0;
    public byte channel2 = 0;
    public byte channel3 = 0;
    public byte channel4 = 0;
    
    //byte[] dmxData = [channel1, channel2, channel3, channel4];

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
